---
title: "Untitled"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}

data1 <- read.delim2("/Users/carolwan/Downloads/CIND820_Project/Netflix_Dataset/combined_data_1.txt", header = FALSE, sep = ',')
data2 <- read.delim2("/Users/carolwan/Downloads/CIND820_Project/Netflix_Dataset/combined_data_2.txt", header = FALSE, sep = ',')
data3 <- read.delim2("/Users/carolwan/Downloads/CIND820_Project/Netflix_Dataset/combined_data_3.txt", header = FALSE, sep = ',')
data4 <- read.delim2("/Users/carolwan/Downloads/CIND820_Project/Netflix_Dataset/combined_data_4.txt", header = FALSE, sep = ',')
rating <- rbind(data1, data2, data3, data4)
headers <- c("CustomerID", "Rating", "Date")
colnames(rating) <- headers
head(rating)


```

```{r}


```

